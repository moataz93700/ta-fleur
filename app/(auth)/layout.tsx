/*
  AUTH LAYOUT — TA FLEUR
  Pas de header/footer : expérience immersive dédiée.
*/

import type { ReactNode } from 'react'

export default function AuthLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
